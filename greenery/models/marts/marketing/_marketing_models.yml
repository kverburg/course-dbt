version: 2

models:
  - name: fact_user_orders
    description: "Fact table for order on the user level"
    columns:
      - name: user_id
        description: "The primary key for this table"
        tests:
          - unique
          - not_null
