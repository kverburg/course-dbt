version: 2

models:
  - name: int_user_orders
    description: "Intermediate model for orders per user"
    columns:
      - name: user_id
        description: "The primary key for this table"
        tests:
          - unique
          - not_null